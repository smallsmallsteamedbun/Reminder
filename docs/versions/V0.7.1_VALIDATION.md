# Reminder `0.7.1` 验证记录

- 版本：`0.7.1`
- 验证日期：2026-08-03
- 状态：自动验证通过

## 已完成范围

- 事件从关闭切换为开启时，同时解除该事件已有的单项暂停并恢复运行。
- 固定时间事件从冻结的剩余时间继续计时。
- 指定时间事件恢复日历调度。
- 全部暂停期间重新开启一个事件时，只恢复目标事件，其他事件继续保持暂停。
- 卸载数据选择改用 Windows 原生 Task Dialog。

## 自动验证

- Debug 构建：通过，0 警告、0 错误。
- Release 自包含发布：通过。
- Logic、时间计划、不可提醒状态、持久化、通知设置与搜索、主题与启动共 `96` 项回归通过。
- 项目版本、应用界面版本常量、需求当前版本、版本目录索引、安装器默认版本和版本历史已同步为 `0.7.1`。

## 仍需人工确认

- 在用户当前桌面环境中确认 Windows 原生 Task Dialog 的视觉尺寸与“保留 Data”“删除 Data”“取消卸载”三个按钮。
- 安装包仍未配置代码签名；正式公开分发可能出现“未知发布者”或 SmartScreen 提示。
