# 用户消息记录

- 会话 ID：2026-07-28-003-render-mode-setting
- 开始日期：2026-07-28
- 主题：渲染模式设置与重启切换

## U001

- 接收时间：2026-07-28，具体时间未知
- 附件：无

~~~~text
用户认为硬件渲染理论上效果更好，但当前电脑上的空白 WPF 硬件渲染就占用约 1 GB 内存，无法接受，并质疑这是否是因为电脑没有独立显卡。用户以后可以在有独立显卡的电脑上继续测试。

用户询问：

- 是否可以在软件设置中提供硬件渲染和软件渲染切换；
- 是否有软件默认使用兼容性更高的软件渲染；
- WPF 是否能在程序运行过程中切换硬件与软件渲染；
- 如果不适合运行时直接切换，能否在用户切换设置后由软件一键自动重启并应用新模式。

当前是在讨论可行性，尚未明确要求立即修改代码。
~~~~

## 会话关闭

- 关闭日期：2026-07-28
- 摘要：`memory/sessions/2026-07/2026-07-28-003-render-mode-setting.md`

## 记录顺序说明

- 追加日期：2026-07-28
- 说明：本文件中的标题编号因早期追加位置出现 `U003` 位于 `U002` 之前的排版顺序；实际接收顺序为 `U001`、`U002`、`U003`。为遵守近原始记录只追加不回写的规则，保留原段落位置并在此说明。

## 会话重新打开

- 日期：2026-07-28
- 原因：用户修正重启提示框的第二个按钮文案，并要求复述下一阶段交付范围。

## U003

- 接收时间：2026-07-28，具体时间未知
- 附件：无

~~~~text
用户将更改渲染模式后的提示框第二个选项从“稍后重启软件”修正为“稍后自行重启”。

用户同时要求说明软件下一个阶段需要实现到什么程度。
~~~~

## 会话关闭

- 关闭日期：2026-07-28
- 摘要：`memory/sessions/2026-07/2026-07-28-003-render-mode-setting.md`

## 会话重新打开

- 日期：2026-07-28
- 原因：用户确认渲染模式的正式需求，并要求把设置页面及其逻辑加入下一阶段。

## U002

- 接收时间：2026-07-28，具体时间未知
- 附件：无

~~~~text
用户正式确认渲染模式需求：

- 设置页提供两个渲染模式选项：“软件渲染（兼容模式）”和“硬件优先渲染”。
- 使用下拉框菜单更改渲染模式。
- 默认选择“硬件优先渲染”。
- 更改渲染模式后弹出提示框，提示“更改此设置软件需要重启”。
- 提示框提供“立即重启软件”和“稍后重启软件”两个选项。
- 选择立即重启后，剩余处理按照软件重启逻辑执行。
- 选择稍后重启后不立即改变当前进程；如果用户之后退出但没有重启，下一次启动时仍必须应用已经更改的渲染模式。

本轮只记录需求，不实施代码。

用户另外要求：

- 重新评估已经确认的主页设计能否支持软件渲染。
- 修改下一阶段范围：设置页不再完全空白，需要实现渲染模式设置页面及其背后逻辑。
- 该功能需要尽快可用，以便测试软件渲染下的内存和 CPU 占用。
~~~~

## 会话关闭

- 关闭日期：2026-07-28
- 摘要：`memory/sessions/2026-07/2026-07-28-003-render-mode-setting.md`
