# 长期精炼记忆

## 项目记忆约定

- 本项目采用项目内 `memory/` 分层记忆系统，用于跨长对话、上下文压缩、中断和跨会话恢复。
- 记忆层分为近原始用户消息、当前工作状态、会话摘要、历史索引、长期精炼知识和有效决策；各层不得相互替代。
- `memory/` 内生成的标题、字段、状态、说明和总结统一使用中文；路径、代码、命令、专有名词等精确信息可保留原文。
- 用户消息默认进入只追加的 `memory/raw/`；以 `[NO_MEMORY]` 开头的消息不记录正文或派生细节。
- 当前协议及完整读写规则见 `AGENTS.md` 的 “Codex Memory Protocol”。
- 来源：`memory/sessions/2026-07/2026-07-22-001-codex-memory-design.md`

## 版本控制约定

- 只有用户明确要求上传时，才能把项目推送或以其他方式同步到 GitHub 等远程仓库；本地编辑、准备提交或本地提交本身不自动授权远程上传。
- 用户执行项目还原时，可能有意连同 `memory/` 一起还原。还原后应重新读取恢复出来的当前记忆、长期记忆、决策和相关会话，并把恢复后的记忆状态视为当前记录，不擅自补回被还原掉的后续记忆。
- 首次 GitHub 存档目标为 `origin/main`，提交消息为“确认需求文档绝大多数逻辑，准备好第一次构建初步简易代码”。
- `docs/versions/VERSION_HISTORY.md` 是简短的用户可见版本记录；特定版本的实施、验证和发布记录集中存放在 `docs/versions/`。每次版本号变化时都要同步项目元数据、程序显示版本、需求当前版本和版本记录。用户要求上传并提到版本更新时，先记录相较上一版本的功能和优化，再使用版本号或用户指定的发布文字提交。
- 来源：`memory/sessions/2026-07/2026-07-23-001-github-archive.md`

## Reminder 项目定位

- Reminder 是面向 Windows 的桌面提醒程序，核心用途是提醒用户休息眼睛、喝水以及处理其他自定义提醒事件。
- 项目重视长期后台运行时的轻量、稳定、低打扰，以及简洁、现代的界面体验。
- 需求和架构将通过多轮讨论逐步确定；`docs/REQUIREMENTS.md` 是持续维护的独立需求文档，只记录已确认需求和用户已确认的初步想法。
- 已确认但不适合堆入主需求文档的提醒状态、边界和示例持续记录在 `docs/REMINDER_LOGIC_DETAILS.md`；该文件用于细化需求，不另行改变需求。
- 单条事件的界面字段、条件化表单、运行状态和全局事件控制记录在 `docs/EVENT_FORM_DESIGN.md`。
- 后续所有已确认的新需求、需求修改和需求补充都必须同步更新 `docs/REQUIREMENTS.md`，不能只保留在聊天或记忆记录中。
- 当前所有提醒采用统一事件模型；初始的“喝水”和“休息眼睛”只是默认关闭、可以删除的示例事件。
- 点击“添加事件”后立即新增一条默认事件；名称默认“新建事件”，使用第一列文本框直接编辑并允许重名。每条事件由不向用户显示的内部 `event id` 唯一识别。
- 当前提醒时间模型分为“从设置时开始的固定间隔”和“指定时间按一次性、每天、每周、每月或每年重复”两类；指定时间允许位于过去，由调度规则处理。
- Windows 通知需要直接提供“完成”“延迟”“跳过本次”；完成后从实际完成时间重新计算固定间隔。
- 通知采用 Windows 原生布局和关闭入口；按钮操作只处理提醒，不显示主面板。
- 轻量化采用事件驱动、单一最近到期计时器和仅在面板可见时刷新倒计时的原则。CPU 目标在界面显示和隐藏时都低于 1%；内存目标为界面显示时低于 160 MB、隐藏时低于 100 MB；安装包以不超过 300 MB 为可接受目标。指标允许弹性，但应尽量满足并继续降低占用。
- 桌面应用框架已先确定为 WPF。通知关闭叉号按“跳过本次”处理；通知自行消失按“延迟”处理，并使用全局延迟时长再次发送同一次提醒。程序必须区分这两种关闭原因。
- 通知自动消失时间和“延迟”时长都属于全局设置：自动消失默认 10 秒，延迟默认 5 分钟，均不再按事件分别设置。
- 开发阶段允许框架依赖构建；正式发布采用 Inno Setup 生成的自包含 `.exe` 安装程序与软件内更新模块，不再采用 MSIX/App Installer，初始不提供便携压缩包。
- 初期测试版本也必须结合未来功能扩建进行架构设计，代码应能作为正式实现基础继续演进，不能采用难以延续的一次性临时结构。
- 初步简易版本以简单 UI 和固定时间间隔事件的最小可运行闭环为目标：添加事件、倒计时、暂停/恢复/重启、隐藏面板后后台运行、Windows 通知及其按钮处理。首版不实现数据保存、启动恢复、完整退出、主题切换和其他完整功能，但仍需保留向完整版本扩展的结构，并验证通知接口能否区分关闭叉号与自动消失。
- UI 保留 Windows 的交互风格与高级感，不完全复刻 Windows；采用顶部页面结构、冷灰底色、低饱和靛蓝主色和自定义卡片层次，不设置宽侧边栏。`0.0.0` 只实现这一方向的单一浅色主题。
- Reminder 程序代码保持为一个 WPF 项目，不按职责拆成多个独立的 Visual Studio 项目；项目内部划分为 `UI`、`Logic`、`Windows`、`System` 四个主要模块。模块调用不采用僵硬的单向限制，但各模块必须保持自身职责；提醒业务数据由 `Logic` 修改，`Windows` 检测系统活动并通知 `Logic`，`UI` 按需读取并显示结果。
- 调度逻辑不直接操作界面或直接调用 Windows 通知；界面不直接修改后台计时器，而是把操作交给 `Logic`。不建立巨大全局变量池，各模块维护自身状态并按需取用；只有版本号等不可变固定信息作为全局共享数据。
- 用户通过托盘主动退出时必须保存所有恢复所需状态。软件未运行期间固定间隔、经过延迟调整的当前剩余时间和限时暂停倒计时全部冻结；启动时优先加载事件和保存时间。指定时间事件在暂停或离线期间到点时记为已跳过。
- 实际睡眠或休眠不再提供任何用户策略，也不在期间发送提醒；唤醒后对期间到点事件提示一次已跳过。息屏和锁屏仍可配置：固定间隔事件采用继续、重新或暂停计时，指定时间事件采用亮屏提醒或抑制后恢复提示。
- 固定间隔在不可提醒期间采用继续计时时，错过通知后仍保持原有时间节奏；“已跳过”通知不显示下一次时间，但循环调度内部仍计算未来计划。
- 同一事件错过多次只汇总一次，不显示次数和下次时间；不同错过事件合并为一条无按钮通知。普通事件同时到点时仍分别通知。
- 真正恢复需要程序运行、用户解锁、事件未单项暂停且全部暂停结束。一次性事件完成或错过后自动关闭并保留在列表。
- 固定时间事件的开关与暂停属性分开：关闭且暂停时直接开启仍保持暂停，点击“恢复”后才继续。指定时间事件不显示单项暂停按钮，开启和关闭直接控制日历调度。成功修改任一事件设置后，事件自动开启并解除单项暂停。
- 延迟操作直接把事件当前剩余时间改为所选延迟时长，不维护独立延迟状态。一次性或已无未来计划的过去事件提示已跳过后关闭，依靠时间、计划和开关避免重复，不增加“该次计划已处理”字段。
- 固定间隔暂停结束后继续暂停前的剩余时间；执行单项或全部重启时，符合条件的固定间隔事件从 `0` 开始新的完整间隔。
- 月度或年度循环遇到本周期不存在的日期时直接跳过该周期；计划时间恰好等于当前时间或真正恢复时刻时按正常到点处理。
- 版本检查只在一天中首次启动时执行一次，检查失败保持安静；用户确认更新后若更新失败，则显示软件内“更新失败”页面。版本信息来源尚未确定，发布方向倾向 GitHub。
- 托盘菜单包含打开主页、快捷全部暂停、全部恢复、版本号和退出；托盘“快捷全部暂停”始终采用“直到手动恢复”。主题默认跟随系统，并要求浅色和深色背景下字体颜色保持清晰。
- 每条事件使用带底框、可横向滚动的双行卡片。默认窗口与默认缩放下全部字段直接显示完整，小窗口或其他缩放仍可横向滚动；动态表单按最大内容固定占位。名称区域需要完整显示达到上限的常见英文、中文或混合名称。固定时间事件第一行包含名称、暂停/恢复、重启、秒级只读倒计时、开关和删除；指定时间事件不显示单项暂停或重启按钮。暂停或关闭的卡片显示得更淡，正常活动事件的“运行中”使用亮浅绿色。第二行包含事件类型动态表单及息屏、锁屏策略。
- 新建自定义事件默认名称“新建事件”、开启、不暂停、采用 45 分钟固定时间事件，终止次数默认“不终止”。事件名称最多 50 个加权字符，ASCII 权重为 1、非 ASCII 权重为 2。指定时间频率为仅一次、每天、每周、每月、每年，切换频率时以当时的一小时之后初始化所需日期和时间字段。
- 息屏、锁屏策略是事件级属性并随事件类型变化；固定时间事件默认“暂停计时”，选择继续计时时默认“不提醒”。实际睡眠或休眠仍是固定行为，不提供事件表单选项。开关和删除适用于全部事件，单项暂停与重启只显示于固定时间事件。
- 所有有效事件都显示精确到秒的下一次提醒倒计时；失效事件通常留空，但每次启动有 5% 概率显示“弹一万遍反方向的钟”。
- 全部暂停对所有事件生效，全部重启只影响符合条件的固定时间事件。
- Windows 原生通知不能为每条事件任意设置整体背景色或文字颜色；Windows 11 仅提供绿色 `Success`、红色 `Critical` 两种按钮语义样式，因此当前不增加“通知颜色”事件属性。原生通知时长只提供 `short`、`long`，精确的全局秒数需要在通知接口选型时验证。
- 删除截止时间设置，只使用“不终止”或 1 至 100 次的终止次数。完成或跳过本次后扣减 1，关闭叉号属于跳过并扣减；延迟和通知自动消失不扣，延迟后的同一次提醒最终只扣 1。纯信息“已跳过”汇总对其中每个不同事件分别扣减 1，同一事件在同一段不可提醒状态中错过多次仍只扣减 1。减到 0 后自动关闭并显示“不终止”。指定时间选择“仅一次”时不显示终止次数设置。
- 息屏和锁屏正式共用一套策略。指定时间事件的频率默认“仅一次”，息屏/锁屏默认“不提醒”。固定时间事件第一行使用六个功能位置，指定时间事件利用隐藏暂停和重启后空出的倒计时区域，但开关和删除位置不变。两个初始示例继续默认关闭，具体预设参数以后再讨论。
- 所有允许用户自定义输入的表单都必须在接受、保存或参与计算前检查合法性；非法格式、异常长度或过大数值不得造成溢出攻击风险、程序崩溃或错误状态。尚未明确的其他字段边界与提示方式后续逐项确定。
- 指定时间采用按频率变化的独立年、月、日、时、分输入框，分钟 `0` 和 `00` 均合法；事件类型使用选中侧带底色的双区域分段选择器。事件名称达到加权上限后直接拒绝继续输入或超限粘贴，首次进入文本框时光标位于已有内容末尾。下拉菜单收起时滚轮不直接切换选项，展开时滚轮只滚动弹出列表；鼠标回到主页滚动时先收起展开列表。事件卡片支持触摸板或系统横向滚轮，外层事件列表继续使用纵向滚轮。
- 已经发出但尚未处理的普通通知如果随后遇到锁屏或者实际睡眠、休眠，按主动延迟相同的规则处理。
- 事件开关使用短暂、按需且遵循 Windows 动画偏好的滑块位移动画；主页面不显示输入应用提示和底部后台运行提示，底部只保留薄分隔边框。
- 当前本地版本号为 `0.4.1`；第一位用于重大架构重构，第二位用于新功能上线，第三位用于当前版本内的 Bug 修复。`0.4.0` 已归档持久化、启动与离线恢复、备份回退、托盘完整退出、低频保存及事件配置和全局控制即时保存。
- 持久化已经采用版本化 JSON：使用 `System.Text.Json` 序列化为 UTF-8 字节，再由 Windows DPAPI 以当前用户范围整体保护；写入通过 `state.tmp` 原子替换 `state.dat`，并以 `state.bak` 保留上一份正式状态。
- Reminder `0.0.0` 初步简易版本已经实现：固定间隔事件、输入校验、倒计时、暂停/恢复/重启、窗口隐藏后台调度、系统托盘和 Windows 通知处理均已形成最小闭环。实施与验证记录见 `docs/versions/V0.0.0_IMPLEMENTATION_PLAN.md`、`docs/versions/V0.0.0_VALIDATION.md`。
- 首版使用应用内一次性计时器控制通知在默认 10 秒后消失，以便将自动消失映射为延迟并与 Windows 关闭叉号的跳过结果区分；完成、延迟、跳过、关闭叉号和自动消失均已做真实通知验证。
- 首版 Debug、Release、Logic、视觉和依赖漏洞检查已通过。非交互测试桌面只能确认隐藏状态约 46.5 MB、空闲 CPU 采样为 0%；界面显示时低于 160 MB 仍需在普通交互桌面复核。
- `0.1.0` 增加事件列表鼠标滚轮、新事件自动定位和边框高亮；固定间隔事件延迟取全局延迟时长与事件间隔中的较小值；通知横幅自动消失后保留在 Windows 通知中心并继续带有操作按钮。事件搜索输入框已确认后续需要，但匹配与交互规则仍待讨论。
- 事件列表滚轮和新增事件定位采用自然启动、减速的平滑动画；新增卡片在定位完成后才高亮。删除按钮使用红色危险样式，删除必须经过“是否删除事件”确认框。
- 通知中心中的保留通知点击正文后重新展示同一提醒横幅，正文点击本身不改变业务状态；重新展示后的完成、延迟和跳过按钮继续有效。
- 所有数据与交互流程必须为未来事件类型、字段、操作、设置和平台结果保留可扩展边界；界面动画、提醒状态、Windows 回调、存储与网络继续由对应模块负责，并通过明确方法、事件或接口传递。
- 删除事件后，原来位于下方的事件卡片应平滑上移填补空位。所有界面动画必须短时、按需且低资源，优先避免逐帧布局重算；动画结束后不得残留空闲计时或渲染回调。
- 主页版本号移动到左上角品牌区域，位于程序图标旁或程序名称旁。
- 完整退出、数据保存、启动恢复和异常恢复已经在事件模型稳定后完成首轮实现；托盘退出保存成功后显式关闭，窗口右上角关闭仍只隐藏。
- `0.1.1` 已整理平滑滚动与定位、删除确认与重排、通知中心正文重新展示以及左上角版本号等改进。后续完整事件时间模型阶段已经移除普通界面动画的固定 60 帧限制；自定义滚动按实际经过时间推进，删除动画仍只移动可见受影响卡片，并在结束或中断后清理状态。
- `0.2.0` 已由用户同步到 `origin/main`：固定间隔与指定时间使用共同事件结构，指定时间支持仅一次、每天、每周、每月和每年，两类事件共用最近到期调度、通知处理与倒计时；可循环事件支持“不终止、1–100 次”，新增日期与时间输入均在进入计算前校验。
- `0.3.0` 纳入不可提醒状态闭环及其实机体验修订：单项暂停、全部暂停和系统状态分层；全部暂停支持手动恢复、1 分钟及 5 分钟至 5 小时的快捷时长；全部恢复和全部重启都按是否开启筛选固定时间事件，并解除这些事件原有的单项暂停；息屏、锁屏和睡眠状态通过独立 Windows 监测器进入逻辑层；错过事件按 ID 去重汇总并扣减终止次数。睡眠固定抑制普通通知并优先于息屏/锁屏策略，未处理通知遇到锁屏或睡眠按延迟处理。
- 收起的下拉菜单不再用滚轮改值，展开列表仍接收滚轮。主页面全部暂停与全部恢复合并为切换按钮，并为全局操作提供一秒悬停说明。Windows 通知状态显示在右下角，未就绪时标红并显示原因和解决办法；程序启动时主动读取一次通知设置，之后每次发送前后继续复检。显示器关闭且事件策略要求提醒时，通知发送前发出一次短时显示请求，不持续阻止息屏、不解锁电脑，也不唤醒实际睡眠。
- 未来界面使用“主页、事件、设置”三个顶部页面。主页以固定位置的大型圆形计时器为中心，右侧沿圆弧显示最多三个最近事件长方形卡片；圆形和卡片均可进入目标事件，事件页继续承载现有双行卡片列表。
- 主页只是逻辑层事件状态的只读映射，不新增业务状态或调度器。圆形周围使用秒、分、时、天多层彩色圆环表达剩余时间分量，不绘制完整背景轨道；超过 365 天使用余数天环加一个完整外环。单项暂停和关闭事件不进入最近候选，全部暂停期间固定事件显示冻结时间，指定时间事件继续显示真实计划并标记到点不提醒。等待通知处理和无运行事件也有独立主页状态；完整细节见 `docs/HOME_PAGE_DESIGN.md`。
- 不可提醒状态使用可替换时间源和单一最近到期计时器，不增加高频轮询。25 项新状态测试、18 项既有时间模型测试、Debug/Release 构建、界面探针、通知状态与短时显示请求冒烟测试、Windows 监测器冒烟测试已经通过；真实锁屏黑屏后的点亮体验和真实睡眠仍需用户在普通桌面复核。
- `Hidden Window: dotnet.exe` 的 `0xe0434352` 弹窗曾由项目外临时测试宿主的未捕获断言异常触发，不是 `Reminder.App.exe` 故障。主要临时测试入口已捕获顶层异常，失败仍输出完整信息并返回非零退出码，但不再逃逸成 Windows 原生应用程序错误。
- `docs/GITHUB_UPLOAD_GUIDE.md` 记录用户自行从 `D:\Reminder` 提交并推送到 `origin/main` 的步骤；该指南不改变助手远程上传必须获得当次明确授权的规则。
- 新建事件、删除事件，或者成功修改事件名称、类型、计划参数、终止次数、开关和息屏/锁屏策略后，立即请求一次不阻塞界面的后台保存；全部暂停、全部恢复、全部重启及全部暂停限时时长设置完成后也立即保存。紧邻请求允许合并，但必须保存最终完整状态。正常运行期间仍约每 1 小时低频保存一次。设置完成后的暂停倒计时流逝、单项暂停、单项恢复、单项重启、完成、延迟、跳过和其他单纯倒计时流逝不要求每次立即写盘；强制退出时允许回退到最近一次即时保存或低频快照。系统时间或时区变化不做额外历史校正，继续按当前时间、计划和开关判断。
- 数据持久化、启动恢复与完整退出已经完成首轮实现；运行数据、临时状态、备份和未来更新下载都位于软件自身目录的相对 `Data` 文件夹中，不在 C 盘其他位置静默创建数据。
- 正式发布已经确定使用 Inno Setup 选择安装目录并生成卸载器，由 Reminder 软件内更新模块下载、校验并静默运行新版安装器，只替换程序文件而保留 `Data`。具体更新库、更新元数据和签名方案仍待发布阶段确认。
- 卸载开始前必须询问用户选择保留或删除 `Data`；保留时只卸载程序内容并留下完整数据目录，删除时同时移除整个 `Data`。
- `state.dat` 损坏或无法载入时自动尝试 `state.bak`；备份也无法恢复时显示独立的恢复失败窗口，用户确认后关闭提示，无效数据不得进入提醒逻辑。
- 新增 13 项持久化测试全部通过，其中包含新建、修改设置、删除、三个全局控制操作和全部暂停时长修改后的即时后台保存；18 项时间模型、25 项不可提醒状态、Windows 监测、连续两次启动恢复、托盘退出、Debug/Release 构建和恢复失败窗口渲染均通过。真实关机保存、超过一小时周期保存及未来安装目录写权限仍需实机验证。
- `0.4.0` 完成后的下一阶段已经确认实现主页与多页面导航，专项计划为 `docs/NEXT_STAGE_HOME_NAVIGATION_PLAN.md`；主页仍只读映射现有逻辑状态，不新增第二套计时或调度状态。设置页本阶段只建立页面容器、导航入口和切换动画，不实现具体设置条目、设置数据或设置业务逻辑。计划已经归档，但必须等待用户完成备份并再次通知后才开始修改代码。
- 固定间隔表单已经改为按合法总时长动态显示：不足 60 分钟只显示分钟，达到 60 分钟显示小时和分钟，达到 24 小时显示天、小时和分钟，最大 365 天且不显示年。各分量允许自动进位；单个空分量按 0 参与整组换算，其余分量合计合法时正常应用，全部为空或合计为 0 时拒绝应用。只有按 Enter 或焦点离开整个固定间隔表单才统一校验、换算和重排，在子输入框之间切换不提交。
- UI 中尚未通过校验的名称、固定间隔或指定时间文本不进入 Logic，也不进入持久化状态。即使非法文本继续留在界面中，其他设置引发的即时保存和低频检查点仍写入该字段最后一次合法值。
- 2026-07-28 本机内存诊断确认：脱离 Visual Studio 的 Debug 和 Release 在 WPF 硬件渲染下都约为 1.1 GB，纯净空白 .NET 10 WPF 窗口也约为 1.04 GB；软件渲染下完整 Reminder 窗口可见约 180–184 MB、隐藏约 19–25 MB，托管堆约 7 MB。异常主要位于本机 WPF 硬件渲染与显卡、驱动或显示环境的组合，不是 Reminder 事件逻辑或 VS 调试器；软件渲染没有改为默认，后续需在 CPU、动画和更多设备之间权衡。详见 `docs/versions/V0.4.0_MEMORY_DIAGNOSTIC.md`。
- `0.4.1` 归档固定间隔动态显示分钟、小时和天、自动进位、整组提交、空分量按 0 以及非法草稿持久化隔离；主页、多页面导航和软件渲染默认策略不属于本版本。
- 来源：`memory/sessions/2026-07/2026-07-22-002-reminder-requirements.md`、`memory/sessions/2026-07/2026-07-24-002-interaction-polish.md`、`memory/sessions/2026-07/2026-07-24-003-next-feature-plan.md`、`memory/sessions/2026-07/2026-07-24-006-v0-1-1-release-preparation.md`、`memory/sessions/2026-07/2026-07-24-007-complete-event-time-model.md`、`memory/sessions/2026-07/2026-07-24-008-event-interaction-form.md`
