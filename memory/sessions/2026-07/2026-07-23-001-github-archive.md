# 2026-07-23 - GitHub 首次归档

- 会话 ID：`2026-07-23-001-github-archive`
- 近原始记录：`memory/raw/2026-07/2026-07-23-001-github-archive.user.md`
- 用户消息范围：U001-U001
- 状态：已完成

## 摘要

用户要求在开始实现 `0.0.0` 代码前，把当前需求、实施准备、项目记忆和 WPF 模板首次提交并同步到 GitHub。仓库的 `origin` 指向 `https://github.com/smallsmallsteamedbun/Reminder.git`，目标分支为 `main`。本次使用用户指定的提交消息完成首次存档，并建立“只有用户明确要求才能远程上传”的长期规则。

## 关键要点

- 本次远程上传由用户明确授权。
- 提交消息固定为“确认需求文档绝大多数逻辑，准备好第一次构建初步简易代码”。
- `.vs/`、`bin/`、`obj/` 和 `*.user` 等 Visual Studio 本机状态与构建输出不进入仓库。
- `docs/`、`memory/`、`AGENTS.md`、解决方案、项目文件和现有 WPF 模板进入首次存档。
- 用户以后会不定期要求同步；未收到明确上传要求时不得自行推送。
- 项目还原可能同时还原 `memory/`，恢复后的记忆状态具有当前效力。

## 变更

- 创建 `.gitignore`，排除 Visual Studio 用户状态、.NET 构建产物和 Windows 外壳元数据。
- 更新 `AGENTS.md`，增加 GitHub 上传授权和项目还原后的记忆处理规则。
- 将当前项目提交到 `main` 并同步到 `origin/main`。

## 决策

- `D-2026-07-23-036`：远程上传必须由用户明确授权；项目还原后以被还原的记忆状态为当前记录。来源为 U001。

## 后续事项

- 等待用户明确要求后再进行下一次 GitHub 同步。
- 等待用户明确要求开始编写 `0.0.0`。

## 不确定项

- 无。
