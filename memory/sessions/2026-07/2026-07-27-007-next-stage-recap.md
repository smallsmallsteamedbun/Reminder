# 2026-07-27 - 确认卸载数据选择并复习下一阶段

- 会话 ID：`2026-07-27-007-next-stage-recap`
- 近原始记录：`memory/raw/2026-07/2026-07-27-007-next-stage-recap.user.md`
- 用户消息范围：U001-U002
- 状态：仅讨论

## 摘要

用户确认卸载 Reminder 时必须询问保留还是删除 `Data`，并要求再次复核下一阶段实施范围。随后确认 `state.dat` 损坏时自动回退到 `state.bak`，两份状态均无法恢复时显示可确认关闭的独立恢复失败窗口。本次只同步已确认文档和规划，没有修改程序代码；下一阶段仍为数据持久化、启动恢复和完整退出，等待用户完成备份后再实施。

## 关键要点

- 卸载流程不能擅自保留或删除运行数据，必须由用户明确选择。
- 选择保留时卸载程序本体但留下完整 `Data`；选择删除时同时移除完整 `Data`。
- 下一阶段不提前实现主页、安装器或自动更新，先闭环状态保存、恢复与进程生命周期。
- `state.dat` 无效时自动尝试 `state.bak`；备份也无效时显示独立的恢复失败窗口，用户确认后关闭提示。
- 用户完成备份并再次通知前，不修改程序代码。

## 变更

- 更新 `docs/REQUIREMENTS.md`，补充卸载选择和用户流程，并从待讨论事项移除该问题。
- 更新 `docs/NEXT_STAGE_PERSISTENCE_PLAN.md`，明确安装、更新与数据边界中的卸载行为。
- 补充正式状态、备份状态和恢复失败提示窗口的启动恢复顺序。
- 未修改业务代码、项目版本或发布配置。

## 决策

- 卸载开始前询问用户选择“保留 Data”或“删除 Data”（来源：U001）。
- 正式状态加载失败时自动尝试备份；备份也失败时显示可确认关闭的恢复失败窗口（来源：U002）。

## 后续事项

- 等待用户完成备份并明确通知开始实施。
- 下一阶段依次建立状态合同、受保护文件存储、启动恢复、完整退出和恢复回归。
- 实现时确定 `state.bak` 的具体生成时机、损坏文件保留方式和旧数据迁移规则。

## 不确定项

- 卸载询问框的默认选中项和具体文案尚未确定。
