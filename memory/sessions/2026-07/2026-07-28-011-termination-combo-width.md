# 2026-07-28 - 加宽终止次数下拉框

- 会话 ID：`2026-07-28-011-termination-combo-width`
- 近原始记录：`memory/raw/2026-07/2026-07-28-011-termination-combo-width.user.md`
- 用户消息范围：U001-U001
- 状态：已完成

## 摘要

加宽终止次数下拉框，使收起状态和展开菜单中的“不终止”“100 次”能够完整显示。为保持默认事件卡片总宽不变，从相邻计划表单列回收了相同宽度，没有新增默认横向滚动。

## 关键要点

- 终止次数控件从 104 调整为 120。
- 对应表单列从 108 调整为 124，计划表单列从 536 调整为 520，卡片内容总宽仍为 1092。
- 固定时间事件和指定时间事件共用同一终止次数控件，因此两类事件同时生效。

## 变更

- 修改 `src/Reminder.App/UI/Views/MainWindow.xaml` 的第二行表单列宽和终止次数控件宽度。
- 更新 `docs/REQUIREMENTS.md` 和 `docs/EVENT_FORM_DESIGN.md`。
- Debug、Release 构建通过；“不终止”“100 次”、收起状态、指定时间年度表单、菜单宽度稳定和默认事件卡片视口检查通过。

## 决策

- 终止次数下拉框必须完整显示“不终止”和“100 次”等最长选项。来源：U001。

## 后续事项

- 当前无。

## 不确定项

- 无。
