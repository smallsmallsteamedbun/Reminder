# 2026-07-24 - 实现删除重排并编写上传指南

- 会话 ID：`2026-07-24-004-reflow-and-upload-guide`
- 近原始记录：`memory/raw/2026-07/2026-07-24-004-reflow-and-upload-guide.user.md`
- 用户消息范围：U001-U001
- 状态：已完成

## 摘要

用户要求立即实现删除事件后的卡片重排动画和左上角版本号，同时确认下一阶段完整事件时间模型的功能范围，并要求编写以后自行上传 GitHub 的操作说明。本轮完成实现、文档归档和自动回归，但没有执行 Git 提交或远程上传。

## 关键要点

- 删除确认后，目标卡片短暂淡出；数据删除只触发一次布局，当前可见且位置变化的下方卡片使用 `TranslateTransform` 平滑上移。
- 动画限制最高 60 帧，结束、中断或窗口隐藏时清理动画状态；没有空闲动画计时器。
- 版本号已放到左上角程序名称旁。
- 下一阶段范围正式确认为共同事件结构、动态表单、五种指定频率、终止次数、共用调度与通知、输入校验和日历边界回归。
- `docs/GITHUB_UPLOAD_GUIDE.md` 记录了以“mmmmm”为示例的本地提交、拉取和推送步骤。

## 变更

- 新增 `src/Reminder.App/UI/Interactions/UiMotion.cs` 和 `ListReflowAnimator.cs`。
- 更新主窗口、删除刷新流程和现有平滑滚动动画设置。
- 更新 `docs/REQUIREMENTS.md`、`docs/NEXT_IMPLEMENTATION_PLAN.md` 和 `docs/V0.1.0_VALIDATION.md`。
- 新增 `docs/GITHUB_UPLOAD_GUIDE.md`。

## 决策

- 下一阶段完整事件时间模型范围由用户在 U001 明确确认。
- 本轮只实现删除重排和版本位置，不提前实现下一阶段事件功能。
- 用户以后可以按项目内指南自行上传；助手仍不得在没有明确当次授权时上传。

## 后续事项

- 等待用户验收删除重排动画和版本号位置。
- 用户提出开始下一阶段时，按已确认范围实现完整事件时间模型。

## 不确定项

- 列表虚拟化与容器回收仍属于待验证技术建议，不是本轮实现内容。
