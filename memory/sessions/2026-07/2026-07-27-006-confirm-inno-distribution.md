# 2026-07-27 - 确认 Inno Setup 发布与更新方案

- 会话 ID：`2026-07-27-006-confirm-inno-distribution`
- 近原始记录：`memory/raw/2026-07/2026-07-27-006-confirm-inno-distribution.user.md`
- 用户消息范围：U001-U001
- 状态：已完成

## 摘要

用户确认采用 Inno Setup 自包含安装程序加软件内更新模块，替代原来的 MSIX/App Installer 方向。正式需求先明确可选非系统盘安装目录、正常安装卸载、一键覆盖更新和软件目录内相对 `Data` 等客观目标，再记录由这些需求推导出的 Inno Setup、静默覆盖安装和数据目录权限方案。项目新增数据持久化与生命周期专项规划，并同步正式需求、逻辑细节和后续实施顺序；未修改业务代码。

## 关键要点

- 正式发布继续采用自包含方式，但安装包改为 Inno Setup `.exe`。
- 安装器允许用户选择包括 D 盘在内的目录，并在 Windows 应用管理中注册卸载入口。
- Reminder 自身产生的状态、备份、临时文件和更新下载都使用安装目录内的相对 `Data`。
- 软件内更新模块在用户确认后下载和校验新版安装器，保存状态并退出，再由 Inno Setup 静默覆盖原目录；程序文件更新而 `Data` 保留。
- 如果安装到受保护目录，只允许安装器给独立 `Data` 子目录必要写权限，不能开放程序文件目录。
- 下一阶段仍只实现持久化、恢复和退出；安装包及自动更新代码留到发布阶段。
- 卸载时如何处理 `Data`、版本元数据来源和代码签名方案仍待讨论。

## 变更

- 更新 `docs/REQUIREMENTS.md` 中的版本更新、发布环境、存储位置、后续顺序和待讨论事项。
- 更新 `docs/REMINDER_LOGIC_DETAILS.md` 的状态路径和坏数据拒绝规则。
- 更新 `docs/NEXT_IMPLEMENTATION_PLAN.md` 与 `docs/NEXT_STAGE_AVAILABILITY_PLAN.md`，移除当前路线中的 MSIX。
- 新增 `docs/NEXT_STAGE_PERSISTENCE_PLAN.md`，记录数据合同、相对 `Data`、退出保存、启动恢复、离线处理、安装更新边界、实施顺序和验收标准。
- 未修改业务代码、版本号，没有提交或上传。

## 决策

- 正式发布采用 Inno Setup 自包含安装程序与软件内更新模块，替代 MSIX/App Installer。（来源：U001）
- 软件运行数据和更新下载统一位于安装目录内的相对 `Data`，覆盖更新必须保留该目录。（来源：U001）

## 后续事项

- 等待用户完成 `0.3.0` 备份并授权实施持久化阶段。
- 持久化编码前闭环备份回退顺序和损坏恢复提示。
- 发布阶段再确定更新元数据、代码签名、具体更新模块和卸载数据交互。

## 不确定项

- 卸载时默认保留、删除还是询问处理 `Data` 尚未确认。
- Inno Setup 已确定，软件内更新模块的具体库或自有实现尚未确定。
